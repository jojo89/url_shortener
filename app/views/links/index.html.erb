<h3>Hot Hundred</h3>
<div class="hottest">
  <ol>
   <% @links.each do |link| %>
      <li><b><%=link.original_url%></b> - <a href=<%= link.original_url %> ><%=link.shortened_url(@base_url)%><b> Visit Count <%= link.access_count%><b/></a></li>
    <% end %>
  </ol>
</div>